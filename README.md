
  # FM - Health Hub

  This is a code bundle for FM - Health Hub. The original project is available at https://www.figma.com/design/EjtxcaDEl06kc874YJp9Es/FM---Health-Hub.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  